<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}SCI ZAYS{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {% include 'header.html.twig' %}
      <section class="tables"></br>
      <div class="d-flex justify-content-center align-items-center mb-3">
            <h5 class="text-center">Liste des contrats locations disponibles</h5>
      </div>
        <div class="container-fluid">
          <div class="row">
            <div class="col-lg-12">
              <div class="card">
                <div class="card-header border-bottom">
                   <i class="ri-file-list-line"></i>&nbsp; Liste
                </div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table text-sm mb-0 table-striped">
                      <thead>
                        <tr>
                          <thead class="thead-dark">
                            <tr>
                                <th scope="col" style="width: 10%;">Porte</th>
                                <th scope="col" style="width: 30%;">Locataire(s)</th>
                                <th scope="col" style="width: 20%;">Actions</th>
                            </tr>
                          </thead>
                        </tr>
                      </thead>
                      {% for b in pBails %}
                      <tr>
                      <td>{{ b.appartement.porte }}</a></td>
                      <td>
                          {{ b.locataires[0].nom }} {{ b.locataires[0].prenom }}
                            {% if b.locataires[1] is defined %}
                                et {{ b.locataires[1].nom }} {{ b.locataires[1].prenom }}
                          {% endif %}
                        </a>
                      </td>
                      <td>
                          <button class="btn btn-primary" onclick="genererQuittance()">
                              <a href="{{ path('contratLocationPDF', { 'id': b.id }) }}" style="color: white; text-decoration: none;">
                                  <i class="ri-draft-line"></i>&nbsp; Télécharger contrats
                              </a>
                          </button>
                        </td>
                      </tr>
                      {% else %}
                      <tr>
                          <td colspan="2">Aucun Contrat de location n'a été trouvé.</td>
                      </tr>
                      {% endfor %}
                    </table></br>
                    <button type="button" class="btn btn-secondary mt-3">
                      <a href="{{ asset('baux/listerContratLocationArchives') }}" style="color: white; text-decoration: none;">
                          <i class="ri-archive-line"></i>&nbsp; Contrat de location (Archivés)
                      </a>
                    </button>
                  </div>
                </div>
              </div>
            </div>
</section>
