<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}SCI ZAYS{% endblock %}</title>
    <!-- Google Fonts -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Raleway:300,300i,400,400i,500,500i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">
    {% block stylesheets %}{% endblock %}
</head>
<body>
      {% include 'header.html.twig' %}
      <section class="cards"></br>
      <div class="d-flex justify-content-center align-items-center mb-3">
            <h5 class="text-center"><i class="ri-home-6-line"></i>&nbsp; Liste des immeubles</h5>
      </div>
        <div class="container-fluid">
          <div class="row">
            {% for i in pImmeubles %}
            <div class="col-lg-4 mb-4">
              <div class="card">
                <div class="card-header">
                  <i class="ri-building-line"></i>&nbsp; Immeuble {{ i.nom }}
                </div>
                <div class="card-body">
                  <p class="card-text">Adresse: {{ i.numRue }} {{ i.rue }}, {{ i.copos }} {{ i.ville }}</p>
                  <a href="{{ path('immeubleConsulter', { 'id': i.id }) }}" class="btn btn-primary">Voir les détails de l'immeuble</a>
                </div>
              </div>
            </div>
            {% else %}
            <div class="col-lg-12">
              <p>Aucun immeuble n'a été trouvé.</p>
            </div>
            {% endfor %}
          </div>
        </div>
      </section>
</body>
</html>
